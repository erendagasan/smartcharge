import React, { useState } from "react";

function Header({
  types,
  setTypes,
  dashboard,
  setDashboard,
  profile,
  setProfile,
  pricing,
  setPricing,
}) {

  return (
    <div className="">
      <section id="LOGO">

      </section>

      <section id="MENU">

      </section>

      <section id="LOGOUT LOGIN">
        
      </section>
    </div>
  );
}

export default Header;
